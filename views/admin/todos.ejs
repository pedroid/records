<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>todo</title>
    <!-- 以下引入 react.js, react-dom.js（react 0.14 後將 react-dom 從 react 核心分離，更符合 react 跨平台抽象化的定位）以及 babel-core browser 版 -->
</head>
<body>
<%- include ../partials/navigation.ejs %>
<div class="content">
<a href="new/">new todo</a><br/>
Following are todos:<br/>

<% if(todos_set.length >0){ %>
<% todos_set.forEach(function(todo){ %>
	<%= todo.todo_id %>:<%= todo.todo_name%>(<a href="delete/<%=todo.todo_id%>">delete</a>)<br/>
	
<% }); %>
<% }else{ %>
None.
<% } %>
</div>
</body>

</html>
