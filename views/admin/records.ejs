<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>songla</title>
    <!-- 以下引入 react.js, react-dom.js（react 0.14 後將 react-dom 從 react 核心分離，更符合 react 跨平台抽象化的定位）以及 babel-core browser 版 -->
</head>
<body>
<%- include ../partials/navigation.ejs %>
<div class="content">
Following are records <br/>
<a href = "/admin/records/new"> new records </a>
<br/>
<% if(records_set.length>0){ %>
<% records_set.forEach(function(record){ %>
		<%= record.record_id %>:
		<%= record.owner_id %>
		 <%= record.score%>, 
		<%= record.item_id %>
		<%= record.authenticate_id %>
		(<a href="/admin/records/delete/<%=record.record_id%>">delete</a>)
		<br/>
<% }); %>
<% }else{ %>
None.
<% } %>

</div>
</body>

</html>
